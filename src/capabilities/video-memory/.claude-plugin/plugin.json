{
  "name": "qwen-mm-plugins-video-memory",
  "version": "1.0.1",
  "description": "Qwen-MM-Plugins video-memory — hierarchical graph memory for long-video QA (Root → SuperEvent → MacroEvent → Subgraph with embedding search), exposed as an MCP server.",
  "skills": ["./skill"],
  "mcpServers": {
    "qwen-mm-plugins-video-memory": {
      "command": "uvx",
      "args": ["--from", "qwen-mm-plugins[video-memory] @ git+https://github.com/QwenLM/Qwen-MM-Plugins.git@qwen-mm-plugins-video-memory-v1.0.1", "qwen-mm-plugins-video-memory"]
    }
  }
}
